<div class="uk-card uk-card-default">
    <div class="uk-card-media-top">
        <img src="{{ item.image }}" width="1800" height="1200" alt="">
    </div>
    <div class="uk-card-body">
        <h3 class="uk-card-title">{{ item.title }}</h3>
        <p>{{ item.description }}</p>
    </div>
</div>